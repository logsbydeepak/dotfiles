# dotfiles

## Installation
`brew install stow`

## Usage
- `stow -t $HOME */` for all
- `stow -t $HOME nvim` for individual

- `stow -D -t $HOME */` remove all
- `stow -D -t $HOME nvim` remove individual


## cli
- fnm
- atuin
- nvm
- fzf
- bat
- lazygit
- jq
